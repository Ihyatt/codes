# correct the hash's key and value so the the key matches the first letter of the value

def hash_correct(hash)
    new_hash = {}
    hash.each do |key, value|
       new_hash[value[0].to_sym] = value 
  end 
 new_hash
end
