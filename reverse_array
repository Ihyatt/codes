def reverse!(arr)
( 0..( arr.length - 1 ) / 2 ).each do |beginningIdx| 
    oppositeIdx = (arr.length - 1) - beginningIdx  
    temp=arr[beginningIdx] 
    arr[beginningIdx] = arr[oppositeIdx] 
    arr[oppositeIdx]=temp 
end
  arr 
end
